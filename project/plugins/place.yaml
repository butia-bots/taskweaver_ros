name: place
enabled: true
required: false
description: >-
  A plugin for placing objects in the real world. You must use `grasp` first in order to grasp the object, and then use `navigate` to navigate to the container location, before using `place`.

parameters:
  - name: container_name
    type: str
    required: true
    description: >-
      Specifies on which container the previously grasped object should be placed.

returns: []

configurations:
